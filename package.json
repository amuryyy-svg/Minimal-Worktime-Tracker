{
  "name": "minimal-worktime-tracker",
  "version": "1.2.2",
  "description": "Minimal desktop tracker for work time, daily streaks and visible progress",
  "author": "Codex <codex@openai.com>",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "lint": "node --check src/main.js && node --check src/preload.js && node --check src/renderer/app.js && node --check src/renderer/tracker-core.js && node --check src/renderer/tracker-storage.js && node --check src/renderer/tracker-timer.js && node --check test/tracker-core.test.js",
    "test": "node test/tracker-core.test.js",
    "dist": "electron-builder --win"
  },
  "build": {
    "appId": "com.workflow.minimalworktimetracker",
    "productName": "Minimal Worktime Tracker",
    "directories": {
      "output": "release"
    },
    "files": [
      "src/**/*",
      "Logo.ico",
      "package.json"
    ],
    "npmRebuild": false,
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    },
    "win": {
      "target": [
        "nsis",
        "portable"
      ],
      "icon": "Logo.ico"
    }
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^26.8.1"
  }
}
